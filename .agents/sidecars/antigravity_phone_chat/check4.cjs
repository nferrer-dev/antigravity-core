const fs = require('fs'); const { JSDOM } = require('jsdom'); const html = fs.readFileSync('manual_pending.html', 'utf8'); const dom = new JSDOM(html); const steps = dom.window.document.querySelectorAll('[data-testid=\'user-input-step\']'); steps.forEach((s, i) => { const buttons = Array.from(s.querySelectorAll('button')); if (buttons.length > 0) { const lastBtn = buttons[buttons.length - 1]; console.log('Step ' + i, 'Last button parent HTML:', lastBtn.parentElement.outerHTML); } });
