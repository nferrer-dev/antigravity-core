const fs = require('fs'); (async () => { await fetch('http://localhost:3000/api/orchestrate/send_message', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify({text: 'Msg 3'}) }); await new Promise(r => setTimeout(r, 100)); const res = await fetch('http://localhost:3000/snapshot'); const data = await res.json(); fs.writeFileSync('pending_state.html', data.html); })();
