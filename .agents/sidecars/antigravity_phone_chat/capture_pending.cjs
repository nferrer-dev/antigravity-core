const fs = require('fs'); let interval = setInterval(async () => { try { const res = await fetch('http://localhost:3000/snapshot'); const data = await res.json(); if (data.html.includes('Test4')) { fs.writeFileSync('test4_snapshot.html', data.html); clearInterval(interval); console.log('Saved test4 snapshot!'); process.exit(0); } } catch(e) {} }, 1000); setTimeout(() => process.exit(1), 40000);
