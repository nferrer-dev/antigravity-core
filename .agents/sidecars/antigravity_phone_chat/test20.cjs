const { JSDOM } = require('jsdom'); fetch('http://localhost:3000/snapshot').then(res => res.json()).then(data => { const dom = new JSDOM(data.html); const doc = dom.window.document; const treeWalker = doc.createTreeWalker(doc.body, 4, null, false); let node; while(node = treeWalker.nextNode()) { if(node.nodeValue === 'Test3') { console.log('Test3 parent innerHTML:', node.parentElement.parentElement.parentElement.outerHTML); } } })
