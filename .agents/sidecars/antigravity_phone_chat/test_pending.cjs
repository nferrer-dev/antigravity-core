const { JSDOM } = require('jsdom'); (async () => { try { await fetch('http://localhost:3000/send', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify({action:'type_text', text: 'Test Pending'}) }); await new Promise(r => setTimeout(r, 1000)); const res = await fetch('http://localhost:3000/snapshot'); const data = await res.json(); const dom = new JSDOM(data.html); const doc = dom.window.document; const steps = doc.querySelectorAll('[data-testid="user-input-step"]'); let found = false; steps.forEach(step => { if (!step.closest('[role="article"]')) { found = true; console.log('Found pending message! HTML:'); console.log(step.outerHTML); } }); if (!found) { console.log('No pending messages found.'); } } catch(e) { console.error(e); } })();
