fetch('http://localhost:3000/snapshot').then(res => res.json()).then(data => { const html = data.html; const blocks = html.split('user-input-step'); let withArticle = 0; let withoutArticle = 0; blocks.slice(1).forEach(b => { if (b.includes('role="article"')) withArticle++; else withoutArticle++; }); console.log('With Article:', withArticle, 'Without:', withoutArticle); })
