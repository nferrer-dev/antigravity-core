const fs = require('fs'); const { JSDOM } = require('jsdom'); (async () => { try { await fetch('http://localhost:3000/send', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify({action:'type_text', text: 'Test Pending'}) }); await new Promise(r => setTimeout(r, 200)); await fetch('http://localhost:3000/send', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify({action:'click_element', agId: 'none', stableId: 'submit'}) }); await new Promise(r => setTimeout(r, 200)); const res = await fetch('http://localhost:3000/snapshot'); const data = await res.json(); fs.writeFileSync('captured_pending.html', data.html); console.log('Saved to captured_pending.html'); } catch(e) { console.error(e); } })();
