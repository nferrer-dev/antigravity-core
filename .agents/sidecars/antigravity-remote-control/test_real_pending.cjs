const fs = require('fs'); (async () => { await fetch('http://localhost:3000/send', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify({action:'type_text', text: 'Real Pending Test'}) }); await new Promise(r => setTimeout(r, 500)); const res = await fetch('http://localhost:3000/snapshot'); const data = await res.json(); fs.writeFileSync('real_pending.html', data.html); })();
