const fs = require('fs'); const { JSDOM } = require('jsdom'); const dom = new JSDOM(fs.readFileSync('latest_snapshot.html', 'utf8')); const doc = dom.window.document; const allText = doc.body.textContent; console.log(allText.includes('running') ? 'Found running' : 'No running'); console.log(allText.includes('tasks') ? 'Found tasks' : 'No tasks'); const maybePill = Array.from(doc.querySelectorAll('button')).filter(b => b.textContent.includes('task')); console.log('Found ' + maybePill.length + ' task buttons'); maybePill.forEach(b => console.log(b.outerHTML.substring(0, 200)));
