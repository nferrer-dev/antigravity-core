const fs = require('fs'); const { JSDOM } = require('jsdom'); const dom = new JSDOM(fs.readFileSync('latest_snapshot.html', 'utf8')); const doc = dom.window.document; const tooltips = Array.from(doc.querySelectorAll('[data-tooltip-id]')); const stopBtns = tooltips.filter(el => el.getAttribute('data-tooltip-id').startsWith('stop-task')); console.log('Found ' + stopBtns.length + ' stop tasks'); stopBtns.forEach(b => console.log(b.closest('.group') ? b.closest('.group').outerHTML.substring(0, 300) : b.outerHTML));
